define(
function() {
	'use strict';

	/**
	 * ${NAME} model.
	 *
	 * @class ${NAME}
	 * @constructor
	 * @module Models
	 */
	var ${NAME} = function() {

	};

	${NAME}.prototype.add = function(value) {
		this.push(value);
	};

	return ${NAME};
});