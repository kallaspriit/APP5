define(
function() {
	'use strict';

	/**
	 * ${NAME} model
	 *
	 * @class NAME
	 * @constructor
	 * @module Models
	 */
	var ${NAME} = [];

	${NAME}.method = function(value) {
		this.push(value);
	};

	return ${NAME};
});